# CV Studio 前端

计算机视觉任务管理平台前端应用，基于React + Vite + Ant Design构建。

## 🚀 快速开始

### 环境要求
- Node.js 16+
- npm 或 yarn

### 安装依赖

```bash
# 安装依赖
npm install
```

### 启动开发服务器

```bash
# 启动开发服务器
npm run dev
```

应用将在 http://localhost:3000 启动（如果3000端口被占用，会自动选择下一个可用端口）

### 构建生产版本

```bash
# 构建生产版本
npm run build
```

### 预览生产版本

```bash
# 预览构建结果
npm run preview
```

## 📁 项目结构

```
frontend/
├── public/                  # 静态资源
│   ├── assets/             # 图片等静态资源
│   ├── index.html          # HTML模板
│   └── favicon.ico         # 网站图标
├── src/
│   ├── components/         # 通用组件
│   │   ├── Layout/         # 布局组件
│   │   ├── Dataset/        # 数据集组件
│   │   ├── ModelBuilder/   # 模型构建器组件
│   │   ├── Training/       # 训练组件
│   │   ├── Inference/      # 推理组件
│   │   └── Common/         # 通用UI组件
│   ├── pages/              # 页面组件
│   │   ├── Login.jsx       # 登录页
│   │   ├── Dashboard.jsx   # 仪表盘
│   │   ├── Datasets.jsx    # 数据集管理
│   │   ├── Models.jsx      # 模型构建
│   │   ├── Training.jsx    # 训练管理
│   │   ├── Inference.jsx   # 推理页面
│   │   └── Settings.jsx    # 设置页面
│   ├── hooks/              # 自定义Hooks
│   ├── services/           # API服务
│   │   ├── api.js          # API基础配置
│   │   ├── auth.js         # 认证服务
│   │   ├── datasets.js     # 数据集服务
│   │   ├── models.js       # 模型服务
│   │   ├── training.js     # 训练服务
│   │   └── inference.js    # 推理服务
│   ├── store/              # Redux状态管理
│   │   ├── index.js        # Store配置
│   │   ├── authSlice.js    # 认证状态
│   │   ├── datasetSlice.js # 数据集状态
│   │   └── modelSlice.js   # 模型状态
│   ├── utils/              # 工具函数
│   ├── App.jsx             # 主应用组件
│   ├── App.css             # 全局样式
│   └── main.jsx            # 应用入口
├── package.json            # 项目配置
├── vite.config.js          # Vite配置
├── index.html              # HTML模板
└── README.md               # 项目说明
```

## 🛠️ 技术栈

### 核心框架
- **React 18** - 用户界面库
- **Vite** - 构建工具
- **React Router** - 路由管理

### UI框架
- **Ant Design** - UI组件库
- **@ant-design/icons** - 图标库

### 状态管理
- **Redux Toolkit** - 状态管理
- **React Redux** - React绑定

### HTTP客户端
- **Axios** - HTTP请求库

### 开发工具
- **ESLint** - 代码检查
- **Prettier** - 代码格式化

## 🔧 主要功能

### 已实现功能
- ✅ React项目架构搭建
- ✅ 路由系统配置
- ✅ 状态管理设置
- ✅ API服务封装
- ✅ 布局组件
- ✅ 登录页面
- ✅ 路由保护
- ✅ 响应式设计

### 待开发功能
- 🔄 数据集管理界面
- 🔄 模型构建器界面
- 🔄 训练监控界面
- 🔄 推理测试界面
- 🔄 用户设置界面
- 🔄 仪表盘数据展示

## 🎨 设计规范

### 代码风格
- 使用函数组件和Hooks
- 遵循ESLint和Prettier规则
- 使用TypeScript类型提示（计划中）

### 组件规范
- 组件文件使用PascalCase命名
- 组件使用Google风格的JSDoc注释
- 样式文件使用CSS Modules或全局样式

### 状态管理
- 使用Redux Toolkit管理全局状态
- 本地状态使用React Hooks
- 异步操作使用Redux Thunk

## 🔌 API配置

前端应用通过代理连接后端API：

```javascript
// vite.config.js
proxy: {
  '/api': {
    target: 'http://localhost:8000',
    changeOrigin: true,
    secure: false
  }
}
```

### API示例

```javascript
// 获取数据集列表
import { datasetService } from '@services/datasets'

const fetchDatasets = async () => {
  try {
    const response = await datasetService.getDatasets()
    console.log(response)
  } catch (error) {
    console.error(error)
  }
}
```

## 🎯 路由配置

```javascript
// 公开路由
/login - 登录页面

// 受保护的路由
/ - 仪表盘
/dashboard - 仪表盘
/datasets - 数据集管理
/models - 模型构建
/training - 训练管理
/inference - 推理测试
/settings - 系统设置
```

## 🧪 测试

```bash
# 运行测试（计划中）
npm test

# 运行测试覆盖率（计划中）
npm run test:coverage
```

## 📦 构建部署

### 开发环境
```bash
npm run dev
```

### 生产环境
```bash
npm run build
npm run preview
```

### Docker部署（计划中）
```bash
# 构建镜像
docker build -t cvstudio-frontend .

# 运行容器
docker run -p 3000:80 cvstudio-frontend
```

## 🤝 贡献指南

1. Fork 项目
2. 创建功能分支
3. 提交更改
4. 推送到分支
5. 创建 Pull Request

## 📄 许可证

本项目采用 MIT 许可证。

## 📞 联系方式

- 项目维护者：MINGYUz01
- 项目地址：https://github.com/MINGYUz01/cvStudio

---

**开发状态**: 第1天完成 ✅